/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'nome.dominio.com',
            },
        ],
    }
};

export default nextConfig;
